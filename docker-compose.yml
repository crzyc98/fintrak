services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - fintrak-data:/data
    environment:
      - DATABASE_PATH=/data/fintrak.duckdb

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  fintrak-data:
